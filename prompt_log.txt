## Student Name: Mihir Patel
## Student ID: 219695808

================================================================================
LAB 8 PROMPTS
================================================================================

================================================================================
Prompt #1: Initial Problem Definition and Requirements Clarification
================================================================================
Purpose: Establish a clear understanding of the Event Registration with Waitlist 
system before implementation.

Prompt Text:
"I need to implement an Event Registration with Waitlist system. Here are the 
requirements:

PROBLEM SUMMARY:
- Build a Python module that manages registration for a single event with fixed capacity
- Accept registrations until capacity is reached
- Place excess users in a FIFO waitlist
- Automatically promote waitlisted users when registered users cancel
- Prevent duplicate registrations
- Allow users to query their registration status

FUNCTIONAL REQUIREMENTS:
1. Accept and store a fixed capacity (>= 0)
2. Register users until capacity reached
3. Place users exceeding capacity into FIFO waitlist
4. Automatically promote first waitlisted user on registered cancellation
5. Reject duplicate registration (raise DuplicateRequest)
6. Allow status queries (registered/waitlisted/none)
7. Handle user cancellation from either registered or waitlist
8. Handle capacity = 0 correctly

CONSTRAINTS:
- Registered users must never exceed capacity
- Waitlist must maintain strict FIFO order
- Each user can exist in only one state
- No user can appear more than once
- Deterministic behavior across identical operation sequences
- Atomic cancel + promote operation

ACCEPTANCE CRITERIA:
- AC1: Register users until capacity, then add to waitlist (FIFO positions)
- AC2: Cancel registered user promotes earliest waitlist user
- AC3: Duplicate registration attempts raise DuplicateRequest
- AC4: Waitlist cancellation updates positions correctly
- AC5: Capacity = 0 means all users go to waitlist
- AC6: Status query returns correct state and position

Before I start coding, are there any ambiguities I should clarify?"

Response #1 Summary:
The requirements were clear and well-structured. The model confirmed:
- Using Python dataclasses (UserStatus) for type safety
- Using lists for registered/waitlist to maintain order
- Using a dict for O(1) user location lookups
- Exception handling: DuplicateRequest and NotFound
- Snapshot method for debugging/testing

Decision: ACCEPTED - Requirements are complete and unambiguous. Proceed to implementation.

================================================================================
Prompt #2: Data Structure and Implementation Strategy
================================================================================
Purpose: Confirm implementation approach before coding.

Prompt Text:
"Based on the requirements, here's my implementation strategy:

DATA STRUCTURES:
1. self.registered = [] (list of user IDs in order)
2. self.waitlist = [] (list of user IDs in FIFO order)
3. self.user_locations = {} (dict mapping user_id to 'registered'/'waitlisted')
4. self.capacity = int (fixed capacity)

IMPLEMENTATION APPROACH:
- register(): Check for duplicates, add to registered if space, else to waitlist
- cancel(): Find user, remove, and if registered, promote from waitlist
- status(): Look up user location and return UserStatus with position
- snapshot(): Return dict of current registered and waitlist lists

EDGE CASES TO HANDLE:
- Zero capacity (all go to waitlist)
- Multiple cancellations in sequence
- Waitlist user canceling before promotion
- Users re-registering after canceling

Does this approach satisfy all constraints?"

Response #2 Summary:
The approach was confirmed to be sound. Using lists + dict provides:
- O(1) lookups via dict for user_locations
- O(n) removal from list (acceptable for typical event sizes)
- Maintains insertion order (important for FIFO)
- snapshot() can easily copy lists for testing

Decision: ACCEPTED - Proceed with implementation.

================================================================================
Prompt #3: Code Implementation Request
================================================================================
Purpose: Implement the EventRegistration class with all required methods.

Prompt Text:
"Implement the EventRegistration class with:

1. __init__(capacity: int) - Initialize with capacity and data structures
2. register(user_id: str) -> UserStatus 
   - Raise DuplicateRequest if user exists
   - Add to registered if space available
   - Else add to waitlist
3. cancel(user_id: str) -> None
   - Raise NotFound if user doesn't exist
   - Remove from registered/waitlist
   - Promote first waitlist user if registered user cancels
4. status(user_id: str) -> UserStatus
   - Return registered/waitlisted/none with position
5. snapshot() -> dict
   - Return dict with 'registered' and 'waitlist' keys

Please implement without TODOs and make it production-ready."

Response #3 Summary:
Full implementation provided with:
- Proper exception handling (DuplicateRequest, NotFound)
- Clear variable naming and inline comments
- Deterministic FIFO promotion logic
- Dynamic position calculation for waitlist
- Efficient state tracking via user_locations dict

Decision: ACCEPTED - All tests pass. Implementation is complete and correct.

================================================================================
Prompt #4: Repository Management
================================================================================
Purpose: Commit implementation to git repository.

Prompt Text:
"Commit the implementation with:
- Clear commit message
- Only solution.py (not __pycache__)
- Reference all key features implemented"

Response #4 Summary:
Commit created successfully with all key features documented.

Decision: ACCEPTED - Code committed to repository.

================================================================================
LAB 9 PROMPTS (Tasks 6–8)
================================================================================

================================================================================
Prompt #5: Persona-Aware Requirement Review (Task 6)
================================================================================
Purpose: Review system requirements with persona constraints before implementation.

Prompt Text:
"I am building an Event Registration with Waitlist system. Before generating code, I
need you to review my specification for completeness. Here is the context:

SYSTEM DESCRIPTION:
A Python module that manages event registration with fixed capacity, FIFO waitlist,
automatic promotions on cancellation, duplicate prevention, and status queries.

PERSONA 1 - Jane Adams (Primary User):
- Busy professional who trusts automation and expects reliable, predictable behavior.
- Acts quickly and rarely revisits suggestions.
- Needs predictable ordering, automatic conflict handling, limited notification overload.
- Pain points: too many notifications, inconsistent behavior, manual conflict resolution.

PERSONA 2 - Mo Rodriguez (Edge User):
- User with accessibility needs and heightened sensitivity to uncertainty.
- Carefully reviews outputs and seeks explanations for system decisions.
- Needs deterministic behavior, clear explanations, explicit edge case handling.
- Pain points: unexplained changes, silent failures, inconsistent repeated behavior.

CONSTRAINTS DERIVED FROM PERSONAS:
C1: Registered count must never exceed capacity. (Jane – reliability)
C2: Waitlist must preserve FIFO order. (Jane – predictable ordering)
C3: Promotions must be deterministic and immediate. (Jane/Mo – automatic handling)
C4: No duplicate users in the system. (Jane/Mo – consistency)
C5: Every operation must return a concise explanation message. (Jane/Mo – accessibility)
C6: Edge cases must be handled explicitly. (Mo – no silent failure)
C7: No duplicate or redundant messages per operation. (Jane – notification limits)
C8: Identical sequences must produce identical results. (Mo – deterministic behavior)

ACCEPTANCE CRITERIA:
AC1: Registered count <= capacity after any operation.
AC2: Waitlist FIFO order preserved after operations.
AC3: Promotion is immediate after registered cancellation.
AC4: Duplicate registration raises DuplicateRequest.
AC5: Register returns explanation message.
AC6: Cancel returns explanation message.
AC7: Zero capacity means all users are waitlisted.
AC8: Re-registration after cancel is accepted.

Please review this specification and identify:
- Missing constraints
- Ambiguous requirements
- Unhandled edge cases"

Response #5 Summary:
The LLM identified two issues:
1. The Lab 8 implementation returned only UserStatus without explanation messages,
   which does not satisfy Mo's need for clear explanations (C5). Suggested adding an
   explanation field to UserStatus and returning explanation strings from cancel().
2. Re-registration after cancellation was handled implicitly but not documented as an
   explicit requirement. Mo's need for explicit edge case handling (C6) requires it to
   be formally specified.
The LLM also confirmed that the remaining constraints and acceptance criteria were
well-defined and testable.

Decision: ACCEPTED - Updated FR6 to add explanation messages, added EC4 for
re-registration, and added AC8. Documented as Revision Notes 1 and 2.

================================================================================
Prompt #6: Implementation Update (Task 7)
================================================================================
Purpose: Update solution.py to satisfy persona-derived constraints.

Prompt Text:
"Based on the revised requirements, update the EventRegistration class:
1. Add an 'explanation' field to UserStatus (default None).
2. register() should return UserStatus with a concise explanation message.
3. cancel() should return a string explanation instead of None, describing
   what happened including any promotion.
4. All exception messages should include the user ID and describe the error.
5. Maintain all existing behavior: FIFO waitlist, capacity enforcement,
   duplicate prevention, deterministic promotion.
Keep the code clean and well-commented with persona constraint references."

Response #6 Summary:
The LLM provided an updated implementation with:
- UserStatus now has an optional 'explanation' field.
- register() returns messages like "User 'alice' has been successfully registered."
- cancel() returns a string explaining the cancellation and any promotion.
- Exception messages include user IDs and are descriptive.
- All existing constraints preserved.

Decision: ACCEPTED - Implementation satisfies C5, C6, and C7.

================================================================================
Prompt #7: Test Design (Task 8)
================================================================================
Purpose: Create tests that verify persona-derived constraints and acceptance criteria.

Prompt Text:
"Write pytest tests for the updated EventRegistration system. Requirements:
- At least 5 tests total, at least 2 edge case tests.
- Each test must have a comment indicating which constraints and acceptance
  criteria it covers (e.g., '# Covers C1, AC1').
- Test that explanation messages are returned (C5, AC5, AC6).
- Edge case tests for: zero capacity (EC1), re-registration after cancel (EC4),
  multiple sequential cancellations (EC2).
- Verify FIFO order, duplicate prevention, and promotion logic."

Response #7 Summary:
The LLM generated 9 tests covering all constraints:
- test_register_until_capacity_then_waitlist (C1, C2, AC1, AC2)
- test_cancel_promotes_earliest_waitlisted_user (C3, AC3)
- test_duplicate_registration_raises_error (C4, AC4)
- test_explanation_messages_are_returned (C5, AC5, AC6)
- test_cancel_waitlisted_user_returns_explanation (C5, C7, AC6)
- test_edge_case_zero_capacity (C6, AC7) — edge case
- test_edge_case_reregister_after_cancel (C6, AC8) — edge case
- test_edge_case_multiple_cancellations (C6, C8) — edge case
- test_edge_case_cancel_nonexistent_user (C6) — edge case

Decision: ACCEPTED - All constraints and acceptance criteria are covered with
traceability comments.

================================================================================
Prompt #8: Test Debugging (Task 8)
================================================================================
Purpose: Run tests and verify all pass.

Prompt Text:
"Run the test suite and check if all tests pass. If any fail, identify
whether the failure is due to an implementation issue or a missing requirement."

Response #8 Summary:
All 9 tests passed. The updated solution correctly handles explanation messages,
edge cases, and persona constraints. No additional requirement revisions needed.

Decision: ACCEPTED - All tests passing. Added Revision Note 3 documenting that
C8 (deterministic repeated behavior) was confirmed by testing but was not
previously an explicit constraint.

================================================================================
FINAL STATUS: COMPLETE
================================================================================
All persona-derived constraints implemented and tested.
Requirements, solution, and tests are aligned and traceable.
